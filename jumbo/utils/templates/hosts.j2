{% for host in hosts %}
{{ host.name }} ansible_host={{ host.ip }} ansible_user=vagrant
{% endfor %}

{% for name, hosts in groups.items() %}

[{{ name }}]
  {% for host in hosts %}
{{ host }}
  {% endfor %}
{% endfor %}