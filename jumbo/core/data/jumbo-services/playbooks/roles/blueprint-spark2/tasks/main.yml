--- 
- name: set Spark2 blueprint path
  set_fact:
    blueprint_conf: "{{ role_path }}/template/spark2-blueprint.j2"
    
# Extra variables not generated by Jumbo
# Lighten the blueprint for more readability
#
# - name: set variable used in spark2-blueprint.j2
#   set_fact:
#     metrics:
#       foo: "bar"

- name: Registering service Spark2
  debug:
    msg: "Added {{ blueprint_conf }}"
  changed_when: true
  notify: "add service to blueprint"

- meta: flush_handlers

